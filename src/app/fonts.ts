import { Shrikhand, Sintony } from 'next/font/google';

export const shrikhand = Shrikhand({
  weight: '400',
  style: 'normal',
  subsets: ['latin']
});

export const sintony = Sintony({
  weight: '400',
  style: 'normal',
  subsets: ['latin']
});
